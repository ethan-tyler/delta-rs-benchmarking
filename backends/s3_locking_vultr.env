# Vultr private-network S3-compatible object storage + lock table profile
# Values can be overridden via --storage-option KEY=VALUE in scripts/bench.sh or compare_branch.sh
AWS_REGION=us-east-1
table_root=s3://delta-bench/private
AWS_S3_LOCKING_PROVIDER=dynamodb
DELTA_DYNAMO_TABLE_NAME=delta_bench_lock
